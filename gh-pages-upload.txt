Write-Host "Limpando a bagunça..." -ForegroundColor Green

$filesToDelete = @(".gitgnore", "App.tsx", "index.tsx", "package.json", "tsconfig.json", "types.ts", "vite.config.ts", "package-lock.json", "index.html", "App.tsx")

foreach($file in $filesToDelete){
    if(Test-Path $file){
        Remove-Item -Path $file -Force
        Write-Host "Apagando: $file" -ForegroundColor Red
    }

    Write-Host "task finished. goodbye..." - Cyan
}
